include:
  - docker-compose-caddy.yaml

volumes:
  django_static_root: {}
  grafana_data:
  postgres_data: {}
  prom_data:

services:
  django: &django
    image: bridge-django
    build:
      context: ..
      dockerfile: ./server/Dockerfile
      labels:
         info.offby1.bridge.git-version: ${GIT_VERSION:-}

    # So I can run e.g. `py-spy top --pid 21` in a shell in the container
    cap_add:                    # for [py-spy](https://github.com/benfred/py-spy?tab=readme-ov-file#how-do-i-run-py-spy-in-kubernetes)
      - SYS_PTRACE

    depends_on:
      # https://docs.docker.com/reference/compose-file/services/#long-syntax-1
      django-collected-static:
        condition: service_completed_successfully
      django-migrated:
        condition: service_completed_successfully
      django-oauth-setup:
        condition: service_completed_successfully
      postgres:
        condition: service_started
      redis:
        condition: service_started
    environment:
      COMPOSE_PROFILES: ${COMPOSE_PROFILES:-}
      DJANGO_SECRET_FILE: /run/secrets/django_secret
      DJANGO_SKELETON_KEY_FILE: /run/secrets/django_skeleton_key
      GOOGLE_OAUTH_CLIENT_ID_FILE: /run/secrets/google_oauth_client_id
      GOOGLE_OAUTH_CLIENT_SECRET_FILE: /run/secrets/google_oauth_client_secret
      DJANGO_SETTINGS_MODULE: ${DJANGO_SETTINGS_MODULE:-}
      DOCKER_CONTEXT: ${DOCKER_CONTEXT:-} # so each machine "knows" where it is -- orbstack, hetz, ls, &c
      HOSTNAME: ${HOSTNAME:-} # for Tailscale hostname detection in setup_oauth
      PGHOST: postgres
      PGPASS: postgres
      PYROSCOPE_HOST: pyroscope
      REDIS_HOST: redis

    ports:
      - "127.0.0.1:9000:9000"
    secrets:
      - django_secret
      - django_skeleton_key
      - google_oauth_client_id
      - google_oauth_client_secret
    init: true                  # https://docs.docker.com/reference/compose-file/services/#init
    volumes:
      - django_static_root:/bridge/project/static_root

    labels:
      caddy: ${CADDY_HOSTNAME:-}
      caddy.reverse_proxy: "{{upstreams 9000}}"
      caddy.import: ratelimit   # per-IP rate limit; snippet defined in caddy/Caddyfile

    restart: unless-stopped
    develop:
      # Create a `watch` configuration to update the app
      # https://docs.docker.com/compose/file-watch/#compose-watch-versus-bind-mounts
      watch:
        # Sync the working directory with the `/app` directory in the container
        - action: sync+restart
          path: ./project
          target: /bridge/project
  bot:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "cheating_bot"]
    ports: []
    labels: {}
    depends_on:
      - django

  # Signups close and play completes at wall-clock times; this is what notices.
  # Tournaments do not progress past their deadlines while it is down.
  clock:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "tournament_clock"]
    ports: []
    labels: {}
    depends_on:
      - django

  # Turns database change notifications into SSE broadcasts (see
  # docs/README.listen-notify.md). Its advisory lock makes it a singleton, so a
  # redundant start is harmless.
  notifier:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "notifier"]
    ports: []
    labels: {}
    # The notifier needs the database (to LISTEN) and Redis (to publish SSE), but
    # not the web server -- so it can run alongside a native `just runme` django.
    depends_on:
      - postgres
      - redis


  django-collected-static:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "collectstatic", "--no-input", "--clear"]
    restart: "no"
    ports: []
    depends_on: []

  django-migrated:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "migrate", "--noinput"]
    restart: "no"
    ports: []
    depends_on:
      - postgres

  django-oauth-setup:
    <<: *django
    command: ["uv", "run", "--no-dev", "python", "manage.py", "setup_oauth"]
    restart: "no"
    ports: []
    depends_on:
      django-migrated:
        condition: service_completed_successfully
      postgres:
        condition: service_started

  postgres:
    image: postgres:17
    environment:
      # only for initializing the db
      POSTGRES_DB: bridge
      POSTGRES_PASSWORD: postgres

    ports:
      - "127.0.0.1:5432:5432"

    # https://www.postgresql.org/docs/17/config-setting.html#CONFIG-SETTING-SHELL
    command:
      - "postgres"

      # Experience shows we need at least one connection per player.  (It looks like we need one connection per SSE
      # client)
      - "--max_connections=200"

      - "--log_min_duration_statement=100"

      # like the default, but includes `%a` for the "application name" (into which some middleware smuggles the request
      # ID)
      - "--log_line_prefix='%m [%p] req=%a '"

      # handy for exploring performance
      # - "--log_statement=all"

    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "--username", "postgres"]
      # Runs the healthcheck every 12 hours.  We don't particularly want this, but we are forced to specify *some*
      # interval.
      interval: 12h
      timeout: 10s
      retries: 3

      # This is what we really care about -- it amends the "interval" above, by running the health check every second,
      # stopping after 10 seconds, or if the test succeeds, whichever comes first.
      start_period: 10s
      start_interval: 1s

    restart: unless-stopped

  postgres-exporter:
    profiles:
      - monitoring
    depends_on:
      - postgres
    restart: unless-stopped
    environment:
      - DATA_SOURCE_URI=postgres:5432/postgres?sslmode=disable
      - DATA_SOURCE_USER=postgres
      - DATA_SOURCE_PASS=postgres
    image: quay.io/prometheuscommunity/postgres-exporter

  # Cadged from https://github.com/docker/awesome-compose/blob/master/prometheus-grafana/compose.yaml
  grafana:
    build: grafana
    profiles:
      - monitoring
    depends_on:
      - pyroscope
      - prometheus

    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
    environment:
      - GF_DATE_FORMATS_DEFAULT_TIMEZONE=UTC
      - GF_SECURITY_ADMIN_PASSWORD=grafana
      - GF_SECURITY_ADMIN_USER=admin
    volumes:
      - grafana_data:/var/lib/grafana # saves preferences, I hope

  prometheus:
    build: prometheus
    profiles:
      - monitoring
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'

    depends_on:
      - postgres-exporter

    ports:
      - "127.0.0.1:9090:9090"
    restart: unless-stopped
    volumes:
      - prom_data:/prometheus

  pyroscope:
    profiles:
      - monitoring
    image: grafana/pyroscope
    ports:
      - "127.0.0.1:4040:4040"

  redis:
    image: redis:latest
    ports:
      - "127.0.0.1:6379:6379"
    restart: unless-stopped

secrets:
  django_secret:
    environment: "DJANGO_SECRET_KEY"
  django_skeleton_key:
    environment: "DJANGO_SKELETON_KEY"
  google_oauth_client_id:
    environment: "GOOGLE_OAUTH_CLIENT_ID"
  google_oauth_client_secret:
    environment: "GOOGLE_OAUTH_CLIENT_SECRET"
